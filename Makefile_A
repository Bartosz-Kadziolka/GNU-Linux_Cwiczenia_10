# Makefile (część A) — uzupełnij reguły
# Cel: zbudować program "hello" z pliku hello.c
# Wskazówka: użyj gcc i flag -Wall -Wextra -O2

hello: hello.c
	gcc hello.c -o hello -Wall -Wextra -O2
# TODO: target clean
clean:
	rm -f hello.o

